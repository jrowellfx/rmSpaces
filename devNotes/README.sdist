### WIP ### This is all going to change. Going to redo 
this entire repo and rename it, using makefiles to install 
the utils.

#### Ignore the stuff below.

# To create tarball

cd $HOME
python3 --version

# First STEPS.
#
    python3 -m venv venv
    source venv/bin/activate
    python3 -m pip install --upgrade pip
# else
    source venv/bin/activate

cd <dirWithGitRepos>/rmSpaces

python3 setup.py sdist

cd dist
tar -xzvf rmspaces-2.0.0.tar.gz
cd rmspaces-2.0.0/
